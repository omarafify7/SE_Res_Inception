apiVersion: v1
kind: Service
metadata:
  name: inference-service
  namespace: ml-inference
  labels:
    app: inference
spec:
  type: ClusterIP
  selector:
    app: inference
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
